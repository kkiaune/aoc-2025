const fs = require("fs");
let input = fs.readFileSync("example.txt", "utf-8");
input = input.split(/\r?\n/);

const firstSolution = () => {};

console.log("first part result:", firstSolution());

const secondSolution = () => {};

console.log("second part result:", secondSolution());
